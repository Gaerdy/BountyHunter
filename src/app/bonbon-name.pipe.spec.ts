import { BonbonNamePipe } from './bonbon-name.pipe';

describe('BonbonNamePipe', () => {
  it('create an instance', () => {
    const pipe = new BonbonNamePipe();
    expect(pipe).toBeTruthy();
  });
});
